architectury {
    common("fabric")
}

dependencies {
    modImplementation "net.fabricmc:fabric-loader:$rootProject.fabric_loader_version"
    modImplementation "dev.tocraft:craftedcore-common:$rootProject.craftedcore_version"
}
